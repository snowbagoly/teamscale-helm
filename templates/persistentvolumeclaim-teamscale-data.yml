apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ tpl .Values.instance.dataVolumeName . | quote }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.volumes.dataSize | quote }}