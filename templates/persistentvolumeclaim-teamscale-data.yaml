apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ tpl .Values.volumes.data . | quote }}
  namespace: {{ .Release.Namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.volumes.data_size | quote }}
