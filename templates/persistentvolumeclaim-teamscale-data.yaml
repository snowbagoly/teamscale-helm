apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.data_volume | quote }}
  namespace: {{ .Values.namespace | quote }}
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.data_volume_size | quote }}
